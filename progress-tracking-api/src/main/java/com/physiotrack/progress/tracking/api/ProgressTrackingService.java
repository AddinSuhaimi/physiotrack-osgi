package com.physiotrack.progress.tracking.api;

/**
 * Public service contract for the progress-tracking module.
 * Keep this bundle "interfaces + DTOs only".
 */
public interface ProgressTrackingService {
    String ping();
}
