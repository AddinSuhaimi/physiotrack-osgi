package com.physiotrack.appointment.api;

/**
 * Public service contract for the appointment module.
 * Keep this bundle "interfaces + DTOs only".
 */
public interface AppointmentService {
    String ping();
}
